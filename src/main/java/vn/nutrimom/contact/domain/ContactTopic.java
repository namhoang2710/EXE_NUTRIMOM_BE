package vn.nutrimom.contact.domain;

/** Chủ đề thắc mắc để admin lọc hộp thư hỗ trợ. */
public enum ContactTopic {
    /** Chính sách, điều khoản, quyền riêng tư. */
    POLICY,
    /** Cách sử dụng app/web. */
    APP_USAGE,
    /** Tài khoản, đăng nhập, thông tin cá nhân. */
    ACCOUNT,
    OTHER
}
